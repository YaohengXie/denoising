[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ecg-pcg-denoise"
version = "2.0.2"
description = "Fixed evaluation and protocol retraining of the M14.3-v2 ECG-PCG-IMU thesis system"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "numpy>=1.24",
  "scipy>=1.10",
  "PyYAML>=6.0",
  "matplotlib>=3.7",
  "torch>=2.1",
]

[project.optional-dependencies]
dev = ["pytest>=7.4"]

[project.scripts]
m143-reproduce = "ecg_pcg_denoise.repro.cli:main"
m143-retrain = "ecg_pcg_denoise.retrain.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"

[tool.setuptools.packages.find]
where = ["src"]
